config { 
  type: 'view',
  tags: ["daily"],
  schema: "dataform_imdb_reporting"
}

select
  tconst as movie_id,
  nconst as actor_id 
from
  ${ ref('title_principals') }
where 
  category = 'actor'